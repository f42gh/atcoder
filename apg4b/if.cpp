#include<bits/stdc++.h>
using namespace std;
int main(){
    int tmp =0;
    if (tmp ==0) {
        cout <<"tmp is zero" <<endl;
    }
    else if (!(tmp ==0) || tmp ==1) {
        cout <<"tmp is one or something without zero" <<endl;
    }
    else {
        cout <<"tmp is not zero and two" <<endl;
    }
}
// if in another if is called "nest"
